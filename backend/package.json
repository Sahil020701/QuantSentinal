{
  "name": "quant-sentinal-backend",
  "version": "1.0.0",
  "description": "Backend engine for Quant Sentinal Indian Stock Market AI Trader",
  "main": "server.js",
  "scripts": {
    "postinstall": "pip install -r requirements.txt || pip3 install -r requirements.txt || true",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mongoose": "^9.9.2",
    "yahoo-finance2": "^2.11.3"
  }
}
